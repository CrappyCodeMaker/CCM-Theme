export { shortPathToVscodeSettings, releaseDir, vscodeDir, settingsFile, HELLO, GOODBYE, START, DONE, ERROR_HEADER } from './utils';
export { runThemeGeneration, runTestColors, generateAllThemes, generateSingleTheme } from './commands';
export { parseCLIArgs } from './arguments';
